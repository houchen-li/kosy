---
applyTo: "**/*"
---
# Security

- Do not hardcode API keys, tokens, passwords, or private endpoints.
- Do not commit real credentials or filled `.env` files.
- Read configuration from environment variables or secure secret stores.
- Keep machine-specific paths out of shared code when possible.
