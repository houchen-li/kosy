# Security

- **Never** embed API keys, tokens, passwords, or private URLs in source code
- **Never** commit real `.env` files or credentials to the repository
- Read secrets from environment variables or secret managers
- For local config, use ignored files that are not checked in
